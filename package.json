{
  "name": "prw_esme",
  "private": true,
  "scripts": {
    "dev": "bun run --hot ./src/index.html",
    "build": "bun build ./src/index.html --outdir=dist --minify --production"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
