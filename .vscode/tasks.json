{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "bun",
			"script": "bun build ./src/index.html --outdir=dist --minify --production",
			"problemMatcher": [
				"$tsc"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"label": "bun: build",
			"detail": "bun run bun build ./src/index.html --outdir=dist --minify --production - package.json"
		}
	]
}
